/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    typedRoutes: true,
  },
};

module.exports = nextConfig;

module.exports = {
  // Other Next.js config options
  serverActions: true,
};
